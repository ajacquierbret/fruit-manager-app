version: "2"
services:
  app:
    build: app
    ports:
      - "5000:5000"
  db:
    image: mysql:5.7
    ports:
      - "32000:3306"
    environment:
      MYSQL_ROOT_PASSWORD: XQmgGcWPkP
    volumes:
      - ./db:/docker-entrypoint-initdb.d/:ro